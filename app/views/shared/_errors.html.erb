<% if object.errors.any? %>
<div class="alert alert-danger">
  <h2><%= pluralize(object.errors.count, "error") %> prohibited from saving:</h2>
  <ul>
    <% object.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
