<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%= form.label :driver_id %>
    <%= form.number_field :driver_id %>
  </div> -->

  <div class="field">
    <%= form.label :driver_id %>
    <%= form.collection_select :driver_id, Driver.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :car_id %>
    <%= form.collection_select :car_id, Car.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :busy_from %>
    <%= form.datetime_select :busy_from %>
  </div>

  <div class="field">
    <%= form.label :busy_to %>
    <%= form.datetime_select :busy_to %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
